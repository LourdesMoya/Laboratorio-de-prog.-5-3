int cols = 20;
int fil = 15; 
float cW; 
float cH; 

boolean[][] estados; 

void setup() { 
size(800, 600); 

cW = width / float(cols); 
cH = height / float(fil); 
estados = new boolean[cols][fil]; 
for (int i = 0; i < cols; i++) { 
for (int j = 0; j < fil; j++) { 
estados[i][j] = false; 
} 
} 
}


void draw() { 
  
background(255); 
for (int i = 0; i < cols; i++) { 
for (int j = 0; j < fil; j++) { 
float px = i * cW; 
float py = j * cH; 

if (estados[i][j] == false) { 
fill(255); 
rect(px, py, cW, cH); } 
else { 

} 
} 

fill(0, 0, 200); 
rect(px, py, cW, cH); 
dibujarEstampa(px, px, cW, cH);
} 
} 
noStroke (200);
noFill(); 
rect(px, py, cW, cH); 

void mousePressed() { 
int columnaClic = int(mouseX / cW); 
int filClic = int(mouseY / cH); 
if (columnaClic >= 0 && columnaClic < cols && filClic >= 0 && filClic < fil) { 
estados[columnaClic][filClic] = ! estados[columnaClic][filClic]; 
} 
} 
void dibujarEstampa(float x, float y, float w, float h) { 
} 
fill(255, 0, 0);  
noStroke(); 
ellipse(x + w/2, y + h/2, w * 0.7, h* 0.7); 


