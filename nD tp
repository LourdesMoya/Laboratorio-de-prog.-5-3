int cantidad = 20; 
float[] barras; 
float anchoBarra; 
void setup() { 
size(800, 400); 

barras = new float[cantidad]; anchoBarra = width / float(cantidad); 

for (int i = 0; i < cantidad; i++) { 
barras[i] = height / 2; 
} 
} 
void draw() { 
background(30); 

} 
float x = i * anchoBarra;
float h = barras[i]; 
fill(100, 200, 255); 
rect(x, height - h, anchoBarra - 2, h); 
if (mousePressed) { 
int indice = int(mouseX/ancho Barra); 

if (indice >= 0 && indice < cantidad) { 
float nuevaAltura = height - mouseY; 
 
nuevaAltura = constrain (nuevaAltura, 0, 
height); 
} 
} 
} 
barras[indice] = nuevaAltura; 
